# Cloudflare Pages 配置
name = "kaojing-monitor"
compatibility_date = "2024-01-01"

# Pages 配置
pages_build_output_dir = "frontend/.next"

[build]
command = "cd frontend && npm install && npm run build"

[build.environment]
NODE_VERSION = "18"
NEXT_PUBLIC_API_BASE_URL = "https://kaojing-api.dangwei121105.workers.dev"
