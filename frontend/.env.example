# API 代理目标（next.config rewrites 使用）
# 本地 Cloudflare Workers 默认端口 8787
API_PROXY_TARGET=http://127.0.0.1:8787

# 服务端直连 API 基址（可选，优先于 rewrites）
# API_BASE_URL=http://127.0.0.1:8787

# 浏览器端 API 基址（一般留空，走相对路径 /api/*）
# NEXT_PUBLIC_API_BASE_URL=

# 强制使用 mock 数据（1=开启）
# USE_MOCK=1
