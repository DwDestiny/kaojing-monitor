# 2026-08-20 工作日志

## P1 扩源 + schema 升级（上午-下午连续推进）

### 扩源 8→28 源（核心交付）
- **调研**：连通性扫描 20/26 可达 → 4 个子代理并行精调 19 源（实测抓取验证），17 源可用、3 源不可用
- **sites.json**：8→28 源（25 启用 + 3 禁用备用），覆盖 24 省级人社厅/考试院 + 全国事业单位招聘网
- **不可用源**：浙江（jcms JS 渲染 API 网关）、河北（Vue SPA JSON API）、黑龙江（无招聘专版）——需 engine API 模式适配
- **engine.js 5 项扩展**：GBK 解码（qgsydw）、pageOffset 页码偏移（贵州/湖北 createPageHTML -1 偏移）、offset 参数分页（河南 offset=30）、日期拆分拼接（辽宁 年+月日）、defaultDate 兜底（四川专题页）
- **测试**：crawlers/test/engine.test.js 20 用例全绿；列表抓取验证 25/25 源有效

### Schema 升级（P1-01，已执行线上 D1）
- announcements 扩展 18 列（考试聚合/时间节点/合规/缺失语义）+ 4 新表（exam_materials/policies/compare/notes）
- 迁移文件 database/migrations/20260820_schema_extend.sql，执行后验证 11 列 + 4 表

### 合规分级（P1-02）
- complianceLevel=safe 全源打标 → process.js 透传 → /api/import 受限源截断 raw_html
- 前端 Footer 免责声明 + 详情页来源/采集时间 + API 透传

### 缺失值语义（P1-05）
- rules-engine 规则8：is_known 四态推导（known/unknown/na，none 保留）
- 前端 formatKnown：未知→"待确认"、na/none→"无"

### 修复
- process.js 增量过滤 const 重新赋值崩溃（Assignment to constant variable）——本地验证时发现

### 提交
- 0ffbb49 feat(P1) + chore(忽略 tmp) 已 push
- GitHub Actions crawler.yml 已手动触发（run 32338114522）做真实全量（25 源 + Ollama 提取 + 自动入库）

### 线上验证（GH Actions 两轮）
- 第一轮 21/25 源成功，62 条自动入库；第二轮 6 条增量入库（总数 204 = 136 旧 + 68 新）
- 数据质量抽查：类型全部有值（事业单位 187/三支一扶 10/教师 2/医疗 2/公安辅警 2/其他 1），地区/日期准确
- **修复**：engine UA 改浏览器 UA——修复北京/天津/内蒙古/qgsydw 的 WAF 403/418
- **已知限制**：5 源（四川/山西/内蒙古/海南/qgsydw）海外 GH runner 不可达（超时/403/418），本地中国网络全部验证通过——需中国区 runner 或 Workers 代理

## VPS 迁移可行性验证（下午，阿里云 ECS）

### 验证结果
- **VPS**：ecs-memori（47.120.20.42，阿里云 ECS iZf8zbfum8v0qiuxhbibkmZ，CentOS 7，1.7G 内存/17G 磁盘，opsuser 身份，密钥 ~/.ssh/id_ed25519_ecs_memori）
- 环境：Docker 26.1.4（已配 daocloud/163 加速器）、无 Node、apps 空、openclaw 已停、无运行容器
- **连通性（Node 20 容器内实测）**：gov.cn 200 ✅（广东/北京人社厅）、ollama.com 200 ✅（LLM 提取通道通）、pages.dev 200 ✅
- **阻塞点**：**workers.dev 被 GFW 双重封锁**（DNS 污染→Facebook/Dropbox IP + SNI 阻断，--resolve 直连 Cloudflare IP 也失败）→ 爬虫在 VPS 上无法 POST /api/import 入库
- 系统 curl 老（7.29）访问部分 gov.cn 报 Empty reply，但 Node TLS 栈无此问题（容器实测 200）

### 入库通道方案（待用户决策）
- **方案 A（推荐）**：API 迁移为 Cloudflare Pages Functions 挂到 kaojing-monitor.pages.dev 域（国内实测可达）——读写全通 + 同源免 CORS
- 方案 B：Worker 绑自定义域名（未备案国际域名走 CF 免费 CDN 国内不稳，不推荐）
- 方案 C：维持 GH Actions + VPS 手动补

## API 迁移 Pages Functions（已完成 ✅ 用户确认方案 A）
- api/src/index.js 12 个 handler 加 export；frontend/functions → 根 /functions 12 个端点文件路由（薄封装复用 handler）
- **关键踩坑**：Pages Git 集成按"项目根"找 functions——放 frontend/ 下 Git 集成识别不到（/api 404）；移到仓库根 + 根 wrangler.toml（pages_build_output_dir=frontend/out + D1/AI/vars）后 Git 集成部署正常
- 前端 api.ts 浏览器端改同域相对路径 /api/*；crawlers 入库 URL workers.dev → pages.dev
- 新 AI_API_TOKEN（da422b80...）配置到 Pages secret + GH secret；旧 token 失效
- 线上验证：读端点 200、import 鉴权 401/200、admin 口令 ok、feedback 提交成功

## 爬虫迁移 VPS（进行中）
- 代码 rsync 到 VPS ~/apps/kaojing-crawler/，Docker node:20-slim 装依赖（npmmirror 加速）
- **VPS 实测 5 个 GH Actions 失败源全部成功**：四川 128/海南 12/山西 18/内蒙古 15/qgsydw 30 → 25 源全覆盖
- cron 已配（每天 2:30/14:30，run.sh：docker 跑 process.js + upload-to-d1.js）
- .env：AI_API_TOKEN 已填，**OLLAMA_API_KEY 待用户提供**（明文）
- 待办：OLLAMA key 填入 → 手动跑一轮验证 → GH Actions crawler.yml 停用（避免双通道重复调 LLM）；旧 worker kaojing-api 可停

## 收尾：停用旧通道（用户确认"没用了就停"）
- GH Actions crawler.yml → disabled_manually ✅
- 旧 worker kaojing-api → wrangler delete 删除 ✅（验证 workers.dev 500）
- Pages API 正常 200；保留 deploy.yml 手动备用通道（active 但需 secrets，不会自动跑）
- **OLLAMA key 部署**：用户确认"用昨天同一个 key"（3289053710...，长度 57）。GH secret 读不到明文 → 临时 workflow 经 SSH stdin 推送到 VPS .env（key 不进日志）；用后删除 workflow + VPS_SSH_KEY/VPS_HOST secrets
- VPS 全量验证 run.sh 手动触发中（25 源 + Ollama 提取 + 入库 pages.dev）

## VPS 全链路验证成功（15:00-15:50 首轮）
- **25/25 源全部成功、0 失败**；增量 184 条新数据 LLM 提取 + 规则清洗（20 处修正）→ pages.dev /api/import 自动入库
- 线上总量 204 → **388 条**；5 个此前缺失源全部补上（四川 127/全国 26/山西 18/海南 7/内蒙古/宁夏等）；类型零 null（事业单位 331/教师 31/三支一扶 10/医疗 9/公安辅警 2）
- cron 每天 2:30/14:30 自动运行（run.sh：docker 跑 process.js + upload-to-d1.js）

## ⚠️ 已知效率问题（待优化）
- **大列表源拖时长**：山东列表 1000 条/页 → 过滤后 745 条详情页全抓（含 2023 年旧公告，6 个月过滤会裁掉）→ 单次运行 40-60 分钟
- 优化方向：①增量过滤（线上已有 URL）提前到详情抓取前 ②列表日期预过滤（>6 个月跳过详情）→ 单次可降到 5-10 分钟
- 待用户确认后实施

## ✅ 爬虫效率优化（用户确认，16:13 实施）
- process.js 流程重构：[2.5] 列表日期预过滤（>6 个月跳过详情）+ [2.7] 增量过滤提前（线上已有 URL 在详情前跳过，只抓新公告）
- **实测：25 源全量 3 分钟跑完（vs 首轮 50 分钟，提速 15x+）**，25/25 成功、14 条增量入库，线上 402 条
- commit `4a1f9c8`（perf: 爬虫流程优化）

## P1C：4 新源接入（用户确认继续推进，16:30-17:00）
- **engine JSON API 模式**（TDD 12 测试）：core/api-json.js 支持形态 A（JSON 数组：河北 rsmhapi data.rows）+ 形态 B（HTML 字符串：浙江 jcms data.html）；buildApiRequest（GET/POST/paramJson 模板/分页注入）；urlTemplate（pageWarp?isId={id}）、publishedDate 字段兼容
- **浙江**（jcms API 网关）：20 条/页，条目无日期用采集日兜底，全链路 18 条
- **河北**（rsmhapi JSON API）：detailInList=true（列表 rows 已含 articleContent 正文，跳过 SPA 壳详情页）；近期栏目无新公告（近 6 个月 0 条，配置正确）
- **黑龙江**（通知公告混发）：单页 15 条，白名单筛招考，全链路 2 条
- **军队文职**（81rc.81.cn wzry 频道）：complianceLevel=restricted 仅索引不存正文，跳过详情抓取，5 条入库；前端详情页显示"信息来源：XX（以官网为准）"
- process.js：restricted/detailInList 跳过详情 + CLI 保护（import 不触发全量）
- **实测：29/29 源全部成功、0 失败、25 条入库，线上 427 条**
- commit `5080b57`

## 科目碎片修复（用户反馈"科目划太碎"，17:00）
- 诊断：线上 38 种科目，18 个 count=1、23 个≤2（长尾严重）
- 根因：SUBJECT_ALIASES 别名表未覆盖新源长尾（辅导员岗位业务/高校辅导员综合能力测试/思政教育工作理论等）
- 修复：rules-engine 扩展归并映射（辅导员/思政/技能/业务岗位/教育理论/常识/综合素养/教育法律类）+ 存量二次归并（fix-subjects-v2.mjs，23 条 UPDATE）
- **效果：38 种 → 16 种，count=1 从 18 → 2**；测试 9 绿；VPS 已同步；commit 已推

## examTime 幻觉修复（用户反馈 9253"考试时间9:00-11:30哪来的"，17:15）
- **用户案例**：9253 浙江文物考古——显示考试时间 9:00-11:30，但原文无笔试（田野考古+面试）
- **根因**：LLM 把"咨询时间：工作日上午9:00-11:30""报名时间…9:00-16:00"误提取为笔试时间；免笔试补标信号没覆盖"直接面试/田野实操测评"考核模式
- **修复**：rules-engine 规则7.5（examTime 语境校验：咨询/报名时间置 null + 免笔试强制清 examTime）+ 规则7 补标信号扩展（直接面试/田野实操）+ process.js 提示词负向约束 + 详情 API 补 examNote 映射（此前详情页拿不到"无笔试"）
- 存量 6 条已清（9253/9238/9246 咨询时间 + 9028/9029/9030 报名时间）；测试 examtime-context 5 绿（总 46 绿）
- **经验**：浙江系公告"咨询时间"模式是高频幻觉源，规则 7.5 已兜底

### 待办/注意
- 山东（1000 条/页）、江苏（301 条/页）列表过大，详情抓取耗时——后续考虑限流详情数量
- 考试聚合 API（P1-06）按 V3 用户决策暂缓，exam_name 列已备
- 本地无 OLLAMA key 时全链路验证会卡在无鉴权请求（fetch 无超时）——真实验证依赖 GH Actions
- 详情页是客户端渲染，免责声明需等 Pages 构建完成后在 HTML 中验证
